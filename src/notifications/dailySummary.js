// src/notifications/dailySummary.js
// Logic for sending daily summary notifications

module.exports = async function sendDailySummary(bot) {
  // TODO: fetch all users, per ciascuno:
  // 1. Ottieni promemoria di oggi e non completati (reminderService.getOverdueReminders)
  // 2. Invia messaggio riepilogo con pulsanti inline
  // Suggerito: schedulare con node-cron
};
