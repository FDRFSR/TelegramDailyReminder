// src/services/voiceService.js
// Gestione aggiunta promemoria tramite messaggi vocali
// TODO: Implementare riconoscimento vocale e parsing testo

module.exports = {
  processVoiceMessage: async (userId, voiceFile, ctx) => {
    // TODO: Scarica il file vocale, invia a un servizio di STT (Speech-to-Text), estrae testo e aggiunge reminder
    // Per ora, rispondi che la funzione è in sviluppo
    await ctx.reply('🎤 Funzione di aggiunta promemoria da messaggio vocale in sviluppo.');
  }
};
