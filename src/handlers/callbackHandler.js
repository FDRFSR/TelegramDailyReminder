// src/handlers/callbackHandler.js
// Questo handler è stato disattivato: nessuna gestione callback inline attiva

module.exports = async function handleCallback(ctx) {
  // Nessuna azione: tutte le tastiere inline sono state rimosse dal bot.
  return;
};
